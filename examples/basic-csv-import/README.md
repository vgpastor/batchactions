# basic-csv-import

End-to-end example using `@batchactions/import` to parse CSV, preview validation results, and process records.

## Run

```bash
cd examples/basic-csv-import
npm install
npm start
```

## What it shows

- Schema validation with email + required fields
- Preview mode with valid/invalid record counts
- Processing callback for valid records
- Final job status + summary output
